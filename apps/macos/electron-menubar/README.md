# macOS Electron menubar transition

This workspace is the transition boundary for the existing Electron-based macOS menubar experience. It contains no delegated runtime scripts until the implementation is moved in a later migration task.
